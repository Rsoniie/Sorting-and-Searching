*******LINEAR SEARCH******TC = O(N)***********
#include <bits/stdc++.h> 
vector<int> valueEqualToIndices(vector<int>& numArray, int n) {
	vector<int> v;
	for(int i=0;i<n;i++)
	{
		if(i == numArray[i])
		{
			v.push_back(i);
		}
	}
	if(v.size() ==0)
	{
		v.push_back(-1);
	
	}
	return v;
}
